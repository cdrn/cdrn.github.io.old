import { FileBundle } from 'arbundles/file';
import { TxDetail } from '../faces/txDetail';
import Bundler from './bundler';
import Blockweave from 'blockweave';
import Transaction from 'blockweave/dist/lib/transaction';
import { JWKInterface } from 'blockweave/dist/faces/lib/wallet';
import Api from 'arweave/node/lib/api';
export declare function showDeployDetails(txs: TxDetail[], wallet: JWKInterface, isFile: boolean, dir: string, blockweave: Blockweave, useBundler?: string, bundler?: Bundler, license?: string, bundlerApi?: Api, bundled?: {
    tx: Transaction;
    bundle: FileBundle;
}, colors?: boolean): Promise<number>;
