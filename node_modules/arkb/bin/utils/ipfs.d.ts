/// <reference types="node" />
export default class IPFS {
    private logs;
    constructor(logs?: boolean);
    deploy(dir: string): Promise<import("ipfs-core-types/src/root").AddResult>;
    hash(data: Buffer): Promise<any>;
}
