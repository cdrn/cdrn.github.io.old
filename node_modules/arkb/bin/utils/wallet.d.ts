import { JWKInterface } from 'arweave/node/lib/wallet';
import Conf from 'conf';
export declare function getWallet(walletPath: string, config: Conf, debug: boolean, colors?: boolean): Promise<JWKInterface>;
