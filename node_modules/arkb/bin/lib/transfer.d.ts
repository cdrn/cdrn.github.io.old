import Blockweave from 'blockweave';
import { JWKInterface } from 'arweave/node/lib/wallet';
import Api from 'arweave/node/lib/api';
export default class Transfer {
    private readonly wallet;
    private readonly blockweave;
    private community;
    constructor(wallet: JWKInterface, blockweave: Blockweave);
    execute(target: string, amount: string, feeMultiplier?: number): Promise<string>;
    withdrawBundler(bundler: Api, amount: number): Promise<string>;
}
