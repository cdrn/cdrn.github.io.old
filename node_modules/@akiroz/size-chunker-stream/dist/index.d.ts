/// <reference types="node" />
import { Transform } from "stream";
export default class SizeChunker extends Transform {
    chunkSize: number;
    fill: number;
    chunks: Buffer[];
    constructor({ chunkSize }: {
        chunkSize: number;
    });
    _transform(chunk: Buffer | string, encoding: string, callback: (err?: Error) => any): void;
}
