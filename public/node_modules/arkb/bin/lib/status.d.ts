import Blockweave from 'blockweave';
export declare function status(txid: string, blockweave: Blockweave): Promise<{
    status: number;
    blockHeight: number;
    blockHash: string;
    confirmations: number;
    errorMessage?: string;
}>;
